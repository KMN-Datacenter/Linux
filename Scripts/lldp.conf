# Manual config JOR: 24JAN26

# Enable LLDP on all interfaces
configure system interface pattern *

# System description shown to neighbors
configure system description "Rak 13, Blade166"

# System name (defaults to hostname if omitted)
configure system hostname server166

# Management address (optional but useful)
# Replace with your management IP
configure system ip management pattern 172.28.6.166

# LLDP timing
configure lldp tx-interval 30
configure lldp tx-hold 4
